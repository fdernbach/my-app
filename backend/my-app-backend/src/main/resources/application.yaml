spring:
  application:
    name: backend
  profiles:
    active: dev
  jackson:
    serialization:
      write-dates-as-timestamps: false
  mvc:
    problemdetails:
      enabled: true

server:
  port: 8080
